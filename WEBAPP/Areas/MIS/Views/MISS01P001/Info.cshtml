@using FluentBootstrap
@using WEBAPP.Helper
@using DataAccess.MIS
@using UtilityLib

@model MISS01P001Model

@{
    ViewBag.Title = "Info";
    Layout = "~/Views/Shared/_LayoutChildPage.cshtml";
}

@using (var form = Html.Bootstrap().Form().SetId("form1").SetHorizontal().Begin())
{
    @form.ValidationSummary(true).AddCss(Css.Hidden)
    @Html.AntiForgeryToken()

    @Html.GetVSMWidgetsBegin(Translation.MIS.MISS01P001.ISSUE_CALCULATE)
    using (var row1 = Html.Bootstrap().GridRow().Begin())
    {
        using (row1.GridColumn(12).SetXs(12).Begin())
        {
            using (var r = Html.Bootstrap().GridRow().Begin())
            {
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Issue No : </b> @Model.NO </label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Application Code :</b> @Model.APP_CODE</label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Response By :</b> @Model.RESPONSE_BY</label>
                }
                using (row1.GridColumn(2).Begin())
                {

                }
                using (row1.GridColumn(1).Begin())
                {

                }
            }

            <br>
            using (var r = Html.Bootstrap().GridRow().Begin())
            {
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Issued by :</b> @Model.ISSUE_BY</label>
                }
                using (row1.GridColumn(3).Begin())
                {
                    <label> <b>Issue Date :</b> @Model.ISSUE_DATE</label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Issue Date Period :</b> @Model.ISSUE_DATE_PERIOD</label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Issued type :</b> @Model.ISSUE_TYPE</label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Module Name :</b> @Model.MODULE</label>
                }

            }

            using (var r = Html.Bootstrap().GridRow().Begin())
            {
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Menu :</b> @Model.MENU</label>
                }
                using (row1.GridColumn(3).Begin())
                {
                    <label> <b>Response Date :</b> @Model.STR_RESPONSE_DATE</label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Program Name :</b> @Model.PRG_NAME</label>
                }
                using (row1.GridColumn(2).Begin())
                {
                    <label> <b>Error No :</b> @Model.ESSR_NO</label>
                }
                using (row1.GridColumn(2).Begin())
                {

                }
            }

            <br>

            using (var r = Html.Bootstrap().GridRow().Begin())
            {
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(4).Begin())
                {
                    <label> <b>Root Cause :</b> @Model.ROOT_CAUSE</label>
                }
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(4).Begin())
                {
                    <label> <b>Solution :</b> @Model.SOLUTION</label>
                }
                using (row1.GridColumn(3).Begin())
                {

                }
            }
            <br>

            using (var r = Html.Bootstrap().GridRow().Begin())
            {
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(4).Begin())
                {
                    <label> <b>Effects :</b> @Model.EFFECTS</label>
                }
                using (row1.GridColumn(1).Begin())
                {

                }
                using (row1.GridColumn(4).Begin())
                {
                    <label> <b>Details :</b> @Model.DETAIL</label>
                }
                using (row1.GridColumn(3).Begin())
                {

                }
            }


            @Html.GetVSMWidgetsEnd()
            using (var r = Html.Bootstrap().GridRow().Begin())
            {
                using (row1.GridColumn(6).Begin())
                {
                    @Html.GetVSMWidgetsBegin(Translation.MIS.MISS01P001.ISSUE_PROPERTY)
                    @*using (row1.GridColumn(12).Begin())
                        {
                            <label> <b>Prioriry :</b> @Model.PRIORITY</label>
                        }*@

using (row1.GridColumn(12).Begin())
{
    using (row1.GridColumn(2).Begin())
    {

    }
    using (row1.GridColumn(3).Begin())
    {
        <label> <b>Prioriry :</b> @Model.PRIORITY</label>
    }
    using (row1.GridColumn(7).Begin())
    {
        <label> <b>Response Target :</b> @Model.RESPONSE_TARGET</label>
    }
}
using (row1.GridColumn(12).Begin())
{
    using (row1.GridColumn(2).Begin())
    {

    }
    using (row1.GridColumn(3).Begin())
    {
        <label> <b>Defect :</b> @Model.DEFECT</label>
                        }
                        using (row1.GridColumn(7).Begin())
                        {
                            <label> <b>Resolution Target :</b> @Model.RESOLUTION_TARGET</label>
                        }
                    }
                    using (row1.GridColumn(12).Begin())
                    {
                        using (row1.GridColumn(12).Begin())
                        {
                            <label> <b>     </b></label>
                        }
                    }
                    using (row1.GridColumn(12).Begin())
                    {
                        using (row1.GridColumn(2).Begin())
                        {
                            <label> <b>Manday :</b></label>
                        }
                        using (row1.GridColumn(2).Begin())
                        {
                            <label> <b>SA :</b> @Model.MAN_PLM_SA</label>
                        }
                        using (row1.GridColumn(2).Begin())
                        {
                            <label> <b>QA :</b> @Model.MAN_PLM_QA</label>
                        }
                        using (row1.GridColumn(2).Begin())
                        {
                            <label> <b>PRG :</b> @Model.MAN_PLM_PRG</label>
                        }
                        using (row1.GridColumn(2).Begin())
                        {
                            <label> <b>PL :</b> @Model.MAN_PLM_PL</label>
                        }
                        using (row1.GridColumn(2).Begin())
                        {
                            <label> <b>DBA :</b> @Model.MAN_PLM_DBA</label>
                        }
                    }
                    @Html.GetVSMWidgetsEnd()
                }


                using (row1.GridColumn(6).Begin())
                {
                    @Html.GetVSMWidgetsBegin(Translation.MIS.MISS01P001.ISSUE_DATETIME)
                    using (row1.GridColumn(12).Begin())
                    {
                        using (row1.GridColumn(2).Begin())
                        {

                        }
                        using (row1.GridColumn(5).Begin())
                        {
                            <label> <b>Target Date :</b> @Model.STR_TARGET_DATE</label>
                        }
                        using (row1.GridColumn(5).Begin())
                        {
                            <label> <b>Close Date :</b> @Model.STR_CLOSE_DATE</label>
                        }
                    }
                    using (row1.GridColumn(12).Begin())
                    {
                        using (row1.GridColumn(2).Begin())
                        {

                        }
                        using (row1.GridColumn(5).Begin())
                        {
                            <label> <b>Deploy PD Date :</b> @Model.STR_RDEPLOY_PD</label>
                        }
                        using (row1.GridColumn(5).Begin())
                        {
                            <label> <b>Deploy QA Date :</b> @Model.STR_DEPLOY_QA</label>
                        }
                    }
                    using (row1.GridColumn(12).Begin())
                    {
                        using (row1.GridColumn(12).Begin())
                        {
                            <label> <b>     </b></label>
                        }
                    }
                    using (row1.GridColumn(12).Begin())
                    {
                        using (row1.GridColumn(2).Begin())
                        {

                        }
                        using (row1.GridColumn(5).Begin())
                        {
                            <label> <b>Comment :</b> @Model.REMARK</label>
                        }
                        using (row1.GridColumn(5).Begin())
                        {

                        }
                    }
                    @Html.GetVSMWidgetsEnd()
                }

            }
        }
    }
}

<script>

    $("label").css({
        fontSize: "18px"
    });
</script>